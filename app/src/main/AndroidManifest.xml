<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.montes8.demosqliteroomjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.HomeActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.RegistrarActivity" />
        <activity android:name=".view.HistorialPedidoActivity" />
        <activity android:name=".view.HistorialActivity" />
        <activity android:name=".view.DetallePedidoActivity" />
        <activity android:name=".view.DetallePlatoActivity"></activity>
    </application>

</manifest>